option('binlocate',
  type: 'combo',
  choices: ['plocate', 'locate'],
  value: 'locate',
  description: 'File search tool'
)

option('repo-version',
  type: 'string',
  value: 'n2026',
  description: 'Repository version segment (e.g., n2026, n27)'
)

option('repo-arch',
  type: 'string',
  value: 'x86_64',
  description: 'Repository architecture segment (e.g., x86_64, x86_64-musl)'
)

option('repo-url-core',
  type: 'string',
  value: '',
  description: 'Override Core repository URL (empty = auto-built from repo-version + repo-arch)'
)

option('repo-url-extra',
  type: 'string',
  value: '',
  description: 'Override Extra repository URL (empty = auto-built from repo-version + repo-arch)'
)

option('repo-url-multilib',
  type: 'string',
  value: '',
  description: 'Override Multilib repository URL (empty = auto-built from repo-version + repo-arch)'
)

option('git-url',
  type: 'string',
  value: 'https://gitlab.com/neonatox-sources',
  description: 'Default GIT source repository URL'
)

option('libc',
  type: 'combo',
  choices: ['glibc', 'musl'],
  value: 'glibc',
  description: 'C library variant (affects NHOHOLD, active repos, multilib)'
)

option('git-branch',
  type: 'string',
  value: '',
  description: 'Default Git branch/tag for super-build (e.g. glibc, musl)'
)

option('static-busybox',
  type: 'combo',
  choices: ['yes', 'no'],
  value: 'yes',
  description: 'Build static BusyBox with musl-gcc (autocontained PATH)'
)

option('static-zstd',
  type: 'combo',
  choices: ['yes', 'no'],
  value: 'yes',
  description: 'Build static zstd with musl-gcc (required for package decompression)'
)

option('use-busybox',
  type: 'combo',
  choices: ['yes', 'no'],
  value: 'no',
  description: 'Enable BusyBox private PATH at runtime (user enables in nhopkg.conf)'
)

option('download-jobs',
  type: 'integer',
  value: 4,
  description: 'Maximum simultaneous binary (.nho) downloads (1 = sequential)'
)
